function blueCircle(diameter){
	$('.wrapper').append("<div></div");
	$('div').addClass("round");
	$('div').css("width",diameter+ 'px');
	$('div').css("height",diameter+'px'); 
}

function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}

